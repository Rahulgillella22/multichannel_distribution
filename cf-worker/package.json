{
    "name": "grabon-supabase-proxy",
    "version": "1.0.0",
    "description": "Cloudflare Worker proxy for Supabase — GrabOn MCP project",
    "private": true,
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "tail": "wrangler tail"
    },
    "devDependencies": {
        "wrangler": "^3.0.0"
    }
}